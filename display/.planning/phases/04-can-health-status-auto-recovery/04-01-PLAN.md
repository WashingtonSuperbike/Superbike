---
phase: 04-can-health-status-auto-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DashboardUI/DashboardUI.h
autonomous: true
requirements:
  - CANU-01
  - CANU-02

must_haves:
  truths:
    - "DashboardState exposes a lock-free can_status field readable by both the CAN task and the dashboard task"
    - "CanStatus enum encodes all four states: NO_DATA (0), RECEIVING, ERR_PASSIVE, BUS_OFF"
    - "Default boot state is NO_DATA without any explicit initialiser (zero-init = ordinal 0)"
    - "DashboardWidgets uses can_icon in place of wifi_icon"
  artifacts:
    - path: "src/DashboardUI/DashboardUI.h"
      provides: "CanStatus enum class and std::atomic<CanStatus> can_status field in DashboardState; can_icon field in DashboardWidgets"
      contains: "enum class CanStatus"
  key_links:
    - from: "src/CAN/CAN_Receive.cpp"
      to: "DashboardState::can_status"
      via: "state->can_status.store(CanStatus::*)"
      pattern: "can_status\\.store"
    - from: "src/DashboardUI/DashboardUI.cpp"
      to: "DashboardState::can_status"
      via: "state.can_status.load()"
      pattern: "can_status\\.load"
---

<objective>
Add the `CanStatus` enum class and `std::atomic<CanStatus> can_status` field to `DashboardUI.h`, and rename the `wifi_icon` widget field to `can_icon`. This header is the shared contract consumed by both `CAN_Receive.cpp` (writes can_status) and `DashboardUI.cpp` (reads can_status, uses can_icon). It must land in Wave 1 so both consumers can compile without changes.

Purpose: Establish the data model contract before either the UI or the CAN driver is modified.
Output: Updated `DashboardUI.h` — CanStatus enum, atomic field in DashboardState, renamed widget field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-can-health-status-auto-recovery/04-CONTEXT.md
@.planning/phases/04-can-health-status-auto-recovery/04-PATTERNS.md

<interfaces>
<!-- Current DashboardUI.h state the executor must see before editing. Key sections: -->

// DashboardState (lines 97-105) — add can_status after sd_started:
struct DashboardState {
    DashboardMotorStats       motor;
    DashboardMotorTemps       temps;
    DashboardBMSStatus        bms;
    DashboardBatteryVoltages  battery;
    DashboardGyroData         gyro;
    DashboardThermistorTemps  thermistors;
    std::atomic<bool> sd_started{};   // <-- template; add can_status after this line
};

// DashboardWidgets status icon fields (lines 151-157):
    lv_obj_t *sd_icon;
    lv_obj_t *wifi_icon;     // <-- rename to can_icon
    lv_obj_t *warning_icon;
    lv_obj_t *info_icon;
    lv_obj_t *temp_warning_icon;
    lv_obj_t *logo_icon;

// Existing #include <atomic> already present at line 18 — no new include needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CanStatus enum class to DashboardUI.h</name>
  <files>src/DashboardUI/DashboardUI.h</files>
  <read_first>
    - src/DashboardUI/DashboardUI.h — read in full before editing; understand the struct ordering (DashboardThermistorTemps is the last sub-struct, DashboardState follows it)
  </read_first>
  <action>
Insert the following enum class definition in `DashboardUI.h` immediately BEFORE `struct DashboardState` (after the closing brace of `DashboardThermistorTemps`):

```cpp
// CAN bus health state — updated atomically by CAN_Receive task, read by dashboard task.
// Underlying type uint8_t guarantees lock-free std::atomic on Xtensa/ESP32-S3.
// NO_DATA = 0 so brace-initialisation of std::atomic<CanStatus>{} gives the correct boot default.
enum class CanStatus : uint8_t {
    NO_DATA     = 0,   // driver running, no decoded frame received yet (boot default)
    RECEIVING   = 1,   // at least one frame decoded within the last 3 s
    ERR_PASSIVE = 2,   // TWAI entered error-passive; recovery in progress
    BUS_OFF     = 3,   // TWAI bus-off; recovery in progress
};
```

Do NOT place this inside any struct. It goes at file scope, above `struct DashboardState`.
  </action>
  <verify>
    <automated>grep -n "enum class CanStatus" src/DashboardUI/DashboardUI.h</automated>
  </verify>
  <done>
    `grep -n "enum class CanStatus"` returns a line before `struct DashboardState`.
    `grep -n "NO_DATA.*=.*0"` returns a match.
    `grep -n "uint8_t"` inside the enum block returns a match.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add can_status atomic field and rename wifi_icon to can_icon</name>
  <files>src/DashboardUI/DashboardUI.h</files>
  <read_first>
    - src/DashboardUI/DashboardUI.h — re-read after Task 1 edits to see exact current state before this edit
  </read_first>
  <action>
Make two edits to `DashboardUI.h`:

**Edit A — DashboardState:** After `std::atomic<bool> sd_started{};` (line ~104), add the following line:

```cpp
    std::atomic<CanStatus> can_status{};  // D-01: lock-free CAN health state; default NO_DATA (ordinal 0)
```

The field uses brace-initialisation `{}` which zero-initialises the atomic, mapping to `CanStatus::NO_DATA` (ordinal 0). This is the same pattern as `sd_started{}`.

**Edit B — DashboardWidgets:** Rename `lv_obj_t *wifi_icon;` to:

```cpp
    lv_obj_t *can_icon;   // D-10: replaces wifi_icon; same x=50 position in status strip
```

The comment on the line changes; nothing else in DashboardWidgets changes. Ensure `wifi_icon` no longer appears anywhere in DashboardWidgets struct.
  </action>
  <verify>
    <automated>grep -n "can_status\|can_icon\|wifi_icon" src/DashboardUI/DashboardUI.h</automated>
  </verify>
  <done>
    `grep` output shows:
    - `std::atomic<CanStatus> can_status{};` present inside DashboardState
    - `lv_obj_t *can_icon;` present inside DashboardWidgets
    - `wifi_icon` does NOT appear anywhere in DashboardUI.h
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CAN task → DashboardState → Dashboard task | can_status written by Core 0 CAN task, read by Core 1 dashboard task without a mutex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `DashboardState::can_status` cross-task write | mitigate | `std::atomic<CanStatus>` with `uint8_t` underlying type — guaranteed lock-free on Xtensa; no additional synchronisation needed |
| T-04-01-02 | Information Disclosure | `CanStatus` enum values visible to all TUs including DashboardUI.h | accept | Header is internal firmware; no external exposure; no PII |
</threat_model>

<verification>
After both tasks complete, verify the header compiles cleanly and the contract is correct:

1. Build the project: `pio run` (or equivalent Arduino/ESP-IDF build) — must produce zero errors related to DashboardUI.h.
2. `grep -n "enum class CanStatus" src/DashboardUI/DashboardUI.h` — must return a line before `struct DashboardState`.
3. `grep -n "std::atomic<CanStatus> can_status" src/DashboardUI/DashboardUI.h` — must return a line inside DashboardState.
4. `grep -n "wifi_icon" src/DashboardUI/DashboardUI.h` — must return NO matches.
5. `grep -n "can_icon" src/DashboardUI/DashboardUI.h` — must return exactly one match inside DashboardWidgets.
</verification>

<success_criteria>
- `CanStatus` enum class with four values (NO_DATA=0, RECEIVING=1, ERR_PASSIVE=2, BUS_OFF=3) and `: uint8_t` underlying type is present at file scope in DashboardUI.h
- `std::atomic<CanStatus> can_status{}` field is present inside `struct DashboardState`, immediately after `sd_started`
- `wifi_icon` field removed from `DashboardWidgets`; `can_icon` field present in its place
- Project builds without errors after this change (downstream files that reference `wifi_icon` will be fixed in Plan 02)
- CANU-01 (data model field) is satisfied by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-can-health-status-auto-recovery/04-01-SUMMARY.md`
</output>
